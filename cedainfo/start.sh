#!/bin/bash
#
# Starts cedainfo Django application. If it is already running then shuts down running instance and restarts.
#

echo "The cedainfodb app should now be started and stopped by the /etc/init.d/cedainfodb script (need to su in order to run)"
